void main() {
  String sentence = "Mobile app is not for the weak";

  //methods;
  print(sentence.toUpperCase);
  print(sentence.toLowerCase);
  print(sentence.length);
  print(sentence.contains("a"));
  print(sentence.replaceAll("a", "@"));
  print(sentence.substring(3, 6));
  print(sentence.trim());
}
